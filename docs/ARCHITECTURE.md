# Архитектура Clever PGP v0.9.3

## Зафиксированные принципы

1. Криптографические операции полностью локальны и не зависят от сервера.
2. Программа распространяется бесплатно и не использует сервер лицензирования или активации.
3. Лицо — фактор биометрического управления доступом, но не источник криптографического ключа.
4. Мастер-ключ генерируется криптографически стойким генератором случайных чисел.
5. Отдельного recovery-файла нет. Во всех режимах создаётся парольный слот мастер-ключа; в режиме «только лицо» он служит аварийным локальным способом.
6. GUI, хранилище, криптографическое ядро и будущий биометрический backend разделены, чтобы перенести приложение на Ubuntu без переделки формата файлов.
7. Поддерживаются только отдельные зашифрованные файлы `.cpgp` и файловые
   контейнеры `.cpgv`. Шифрование Windows, физических накопителей, системных и
   загрузочных разделов, EFI и загрузчика запрещено архитектурой и не входит в
   план развития.

Термин «виртуальный диск Windows» далее означает исключительно временно
подключённое представление обычного файла `.cpgv`, а не системный диск компьютера.

## Поток разблокировки паролем

```text
мастер-пароль + уникальная соль
            ↓
          Argon2id
            ↓
   парольный ключ (KEK)
            ↓
аутентифицированная расшифровка
            ↓
случайный мастер-ключ профиля
```

SQLite хранит только соль, параметры Argon2id и зашифрованный мастер-ключ. После блокировки приложения копия мастер-ключа в объекте сессии перезаписывается. Python не даёт строгой гарантии удаления всех временных копий из памяти; это ограничение MVP и одна из причин будущего выделения критического кода в нативный модуль.

## Шифрование файла

```text
случайный ключ файла ── SecretBox(мастер-ключ) ──> ключ в заголовке
         │
         └── XChaCha20-Poly1305 secretstream ──> блоки шифротекста
```

Формат версии 1:

```text
magic | version | header_length | JSON header | record_length | record | ...
```

JSON содержит только идентификаторы алгоритмов, заголовок secretstream, обёрнутый ключ файла и размер блока. Исходное имя не помещается внутрь заголовка. Сырые байты заголовка используются как associated data каждого блока.

Последний блок имеет обязательный тег `FINAL`. Расшифрование выполняется во временный файл; целевой путь появляется только после проверки тега и отсутствия лишних данных.

## Границы модулей

- `biopgp.core.profile_service` — создание профиля, Argon2id, разблокировка сессии;
- `biopgp.core.storage` — SQLite и миграция схемы;
- `biopgp.core.file_crypto` — версия формата `.cpgp` и потоковая криптография;
- `biopgp.core.block_volume` — независимое аутентифицированное хранение логических блоков `.cpgv`;
- `biopgp.core.block_container` — дерево каталогов, экстенты файлов и чередующиеся состояния метаданных;
- `biopgp.core.container` — прежний компактный исследовательский формат, оставленный только для изолированных тестов и не используемый приложением;
- `biopgp.core.mount` — адаптер WinFsp/FUSE и жизненный цикл подключённого диска;
- `biopgp.core.mount_router` — выбор WinFsp или WinSpd по аутентифицированному назначению заголовка после разблокировки;
- `biopgp.core.winspd` — мост SCSI-команд WinSpd, отдельный фоновый процесс и формат виртуального блочного диска;
- `biopgp.core.windows_storage` — строгая идентификация диска Windows, форматирование, поиск буквы, увеличение NTFS и безопасное отключение;
- `biopgp.core.windows_format` — одноразовый аутентифицированный обмен с минимальным UAC-процессом первого форматирования WinSpd-диска;
- `biopgp.core.windows_resize` — одноразовый обмен с минимальным UAC-процессом для повторно проверяемого расширения NTFS;
- `biopgp.core.disk_control` — аутентифицированный loopback-канал и защищённое состояние активного виртуального диска;
- `biopgp.core.windows_shell` — динамическое меню Проводника, ограниченное буквой активного виртуального диска;
- `biopgp.ui.main_window` — интерфейс без криптографических примитивов;
- `biopgp.shell` и `biopgp.ui.shell_dialog` — отдельный безопасный процесс для команды Проводника;
- будущий `biopgp.biometrics` — камера, embedding, liveness и политика совпадения;
- будущий `biopgp.key_protection` — Windows CNG/DPAPI/TPM и Linux Secret Service/TPM2.

## Интеграция с Проводником Windows

`install_context_menu.ps1` создаёт статические shell verbs в пользовательской ветке `HKCU\Software\Classes`. Команда запускается через `pythonw.exe`, поэтому консольное окно не появляется. Путь выбранного файла передаётся как отдельный заключённый в кавычки аргумент.

После подключения виртуального диска приложение создаёт отдельное пользовательское
shell-меню с условием `AppliesTo` по системному свойству `System.ItemPathDisplay`.
Условие содержит конкретную назначенную букву, поэтому команды не появляются у
обычных NTFS/exFAT-накопителей. При отключении удаляется только дерево
`CleverPGP.SystemMenu`; чужие обработчики Проводника не затрагиваются.
Меню содержит только уже проверенные команды: открыть диск, показать сведения,
открыть настройки доступа Clever PGP и безопасно отключить диск. Сведения
открываются в отдельном компактном read-only окне без запуска главного окна и
без UAC. Для виртуального диска команда требует живую защищённую запись, выполняет
аутентифицированный `ping` host-процесса и повторно проверяет принадлежность
устройства Clever PGP; обычный диск Windows отклоняется. Настройки запускаются
напрямую в графическом процессе без `cmd.exe`; после разблокировки профиля
пользователь может изменить режим доступа, зарегистрировать лицо или сменить
мастер-пароль. Команда увеличения появится здесь только после отдельной реальной
UAC-проверки.

Шифрование и расшифрование выполняются в отдельном процессе и отдельном рабочем потоке. Сбой этого процесса не влияет на Проводник. Исходный файл не удаляется; целевой файл появляется только после полной аутентифицированной проверки и атомарного переименования временного файла.

## Следующий этап: биометрический слот

Биометрический backend должен выдавать только решение об успешной локальной проверке. После него системное защищённое хранилище разрешает расшифровать дополнительную обёртку того же мастер-ключа. Шаблон лица хранится локально в зашифрованном виде; камера включается только на время регистрации или проверки.

## Изменение настроек доступа

Смена мастер-пароля не создаёт новый мастер-ключ и не перешифровывает файлы или
контейнеры. После проверки текущего пароля тот же случайный мастер-ключ
защищается новым независимо выработанным парольным ключом и новой солью. Запись
парольного слота заменяется одной транзакцией SQLite с контролем предыдущего
значения, поэтому параллельный процесс не может незаметно перезаписать более
новое состояние. Биометрический слот остаётся неизменным.

Режимы «Только лицо» и «Мастер-пароль + лицо» разрешается включить только после
успешной регистрации лица. В режиме «Только лицо» парольный ввод скрыт в обычном
сценарии, но остаётся доступен как явно выбранный аварийный способ — отдельный
recovery-файл не создаётся.

## Контейнер и виртуальный диск

Формат `.cpgv` версии 3 состоит из трёх слотов защищённого заголовка, двух областей метаданных и массива независимо шифруемых логических блоков размером 4096 байт. В заголовке находятся версия, геометрия диска, идентификатор и случайный ключ тома, защищённый мастер-ключом профиля.

```text
мастер-ключ профиля ──> защищённый случайный ключ тома
                                   │
          ┌────────────────────────┼─────────────────────────┐
          ↓                        ↓                         ↓
  метаданные A              метаданные B            логические блоки
  поколение N               поколение N+1           содержимого файлов
```

Для каждой операции записи формируются новый криптографический параметр блока и уникальный контекст поколения. Идентификатор тома, логический адрес и контекст поколения включаются в аутентифицируемые данные, поэтому повреждение, подмена, перестановка и возврат отдельного блока к прежней версии обнаруживаются при чтении. Перезапись использует копирование при записи: новый блок сохраняется до публикации новой карты размещения. Метаданные кодируют дерево каталогов, размеры и компактные экстенты файлов вместе с ожидаемым контекстом поколения.

Состояния метаданных чередуются. Каждое содержит монотонный номер поколения и контроль целостности всего состояния. Сначала записываются новые блоки, затем неактивное состояние метаданных, после чего выполняется принудительная синхронизация. Если процесс или питание прерваны, при следующем открытии выбирается последнее полностью проверенное поколение. Отдельный резерв блоков сохраняет возможность копирования при записи даже при полностью занятой пользовательской ёмкости; при длительной перезаписи заполненного диска безопасные контрольные состояния создаются порциями.

Слой скрытого диска размещает независимо
аутентифицируемые скрытые блоки внутри свободной хвостовой области открытого
текста внешнего блочного слоя. Внешний слой продолжает аутентифицировать все
физические блоки контейнера, поэтому скрытая запись выполняется как вложенная
операция: шифрование скрытого блока, изменение соответствующего внешнего блока
и его повторная аутентификация. Обёртка защиты внешнего диска отклоняет запись,
пересекающую скрытую область, после чего запрещает остальные записи до
отключения. Открытие разных проекций по двум паролям и непрозрачные заголовки
подключены к пользовательскому сценарию создания нового WinSpd-диска.

Для следующего формата создано хранилище непрозрачных заголовков. Две роли
занимают постоянные области одинакового размера, каждая с двумя копиями,
защищёнными независимо сформированными парольными ключами. В незашифрованном
виде остаются только случайно выглядящие соль, одноразовый параметр и
шифротекст фиксированной длины. Служебная сигнатура, поколение, роль, геометрия
и все ключи находятся внутри аутентифицированной полезной нагрузки. Сначала
проверяется внешняя роль, затем скрытая; результатом является ровно одна
аутентифицированная структура, определяющая дальнейшую блочную проекцию.

Полученная структура уже используется внутренним блочным backend v4. Внешний
ключ открывает массив блоков за фиксированным резервом заголовков, а скрытый
заголовок дополнительно создаёт вложенную проекцию с отдельным ключом и
геометрией. Оба варианта реализуют одинаковый интерфейс чтения, записи и
синхронизации, поэтому драйверу не требуется знать выбранную роль. Защитное
открытие внешней роли оборачивает тот же массив контролем границ скрытого тома.
Внутренний метод добавления скрытого слоя требует переданной вызывающим кодом
доказанно свободной области. Он не используется GUI для существующих дисков.

Пользовательский сценарий v4 создаёт внешний и скрытый слои до
появления файловой системы, затем форматировать внешнюю проекцию только через
обёртку защиты скрытой области. Любая предотвращённая запись делает создание
неуспешным. Добавление скрытого слоя в существующий том остаётся отдельной,
более сложной операцией с эксклюзивным чтением карты занятых кластеров.

Подготовка нового виртуального образа теперь атомарно создаёт обе блочные
проекции и записывает независимую MBR в каждую из них. Внешняя таблица разделов
создаётся через защитную обёртку; любая попытка пересечь скрытую область или
любой иной сбой удаляет ещё не опубликованный образ. Менеджер Windows сначала
подключает и форматирует защищённую внешнюю проекцию, проверяет, что host остался
доступен, безопасно отключает её, затем подключает и форматирует скрытую
проекцию. Пользователю оставляется подключённым только полностью подготовленный
скрытый диск.

В интерфейсе основной пароль выбирает ровно одну проекцию. При открытии внешней
проекции можно дополнительно подтвердить скрытый пароль и включить запрет записи
в скрытую область. Команда увеличения для v4 не публикуется до реализации
атомарного изменения геометрии обеих файловых систем.

Для смены пароля v4 используется идентификатор активной проекции из
DPAPI-защищённой записи фонового host. Текущий пароль должен аутентифицировать
заголовок с тем же идентификатором, а новый не должен открывать ни одну уже
существующую роль контейнера. После этих проверок host безопасно останавливается.
Ключи данных не меняются: увеличивается только поколение выбранного заголовка и
две его копии независимо защищаются новым парольным ключом. Копии записываются
синхронно по одной, поэтому сбой между ними сохраняет доступ хотя бы по одному
из паролей. Успешная операция повторно проверяет новый пароль и отклонение
старого; автоматическое повторное подключение не выполняется.

Пароли в самостоятельный WinSpd-host не передаются. GUI аутентифицирует нужный
непрозрачный заголовок, помещает выбранный материал и при необходимости границы
защиты в одноразовый DPAPI-пакет, а host получает в командной строке только путь
к нему. Пакет удаляется сразу после чтения. Режим передачи случайного
мастер-ключа сохраняется для обычных дисков формата v3; формат v4 используется
для новых дисков со скрытой областью.

Увеличение блочного массива использует отдельные состояния заголовка
`committed → preparing → committed`. Новый рабочий размер публикуется только
после записи и принудительной синхронизации всех добавленных зашифрованных
нулевых блоков. Сбой на любой промежуточной стадии оставляет доступной прежнюю
подтверждённую ёмкость. Существующие блоки, идентификатор и ключ тома при этом не
изменяются. Уменьшение запрещено до появления доказанно безопасного анализа
занятых кластеров файловой системы.

На Windows собственная файловая система пока публикуется через WinFsp; на Ubuntu тот же файловый адаптер сможет использовать системный `/dev/fuse`. Открытые имена и содержимое не записываются во временный каталог. Старый компактный формат с сериализованной SQLite-файловой системой не поддерживается.

При обычном открытии `.cpgv` приложение сначала аутентифицирует заголовок тем же
мастер-ключом профиля, но не читает блоки пользовательских файлов. Точное поле
назначения выбирает WinFsp для собственной файловой системы или WinSpd для
стандартной NTFS/exFAT внутри отдельного виртуального диска. Неизвестное
назначение отклоняется; скрытая
конвертация формата и повторное шифрование не выполняются.

Мост WinSpd уже реализован как самостоятельный скрытый host-процесс. Он
обслуживает чтение, запись, принудительную синхронизацию и освобождение диапазонов
блоков; постоянный идентификатор диска берётся из защищённого заголовка. GUI
создаёт одноразовый запрос с мастер-ключом, защищённым DPAPI текущего пользователя,
а host удаляет запрос сразу после чтения. В командной строке передаётся только
путь к одноразовому запросу. Для команд Проводника host создаёт канал только на
loopback-интерфейсе и принимает команды после проверки случайного 256-битного
токена. Токен, путь контейнера и идентификатор выбранного метода шифрования в
локальном состоянии защищаются DPAPI текущей учётной записи; мастер-ключ в это
состояние не записывается. Защищённый идентификатор позволяет окну сведений
показывать фактический метод подключённого диска без повторного получения
пароля. Официальная проверочная утилита
WinSpd успешно выполняет случайные операции над исходной и упакованной версиями
Clever PGP.

В живом WinSpd-host уже выделенный файл контейнера отображается в системный
кэш страниц Windows. Отображается только шифротекст: расшифрованные блоки
остаются краткоживущими буферами криптографического слоя. Последовательный
диапазон читается одной физической операцией, а проверка блоков выполняется уже
после освобождения блокировки файлового указателя. Это уменьшает стоимость
мелких запросов NTFS без замены WinSpd и криптографического формата. При запросе
`flush` изменённый диапазон отображения и файловый дескриптор принудительно
синхронизируются. Если отображение невозможно, host продолжает работу через
обычный проверенный файловый поток.

Host запускается как отдельный процесс `CleverPGP.exe --winspd-host` (в среде
разработки — через `pythonw.exe`) и не принадлежит GUI через механизм
`multiprocessing`. Поэтому пользователь может выбрать в области уведомлений
«Закрыть Clever PGP — диск останется подключённым». Сам диск продолжает работать
до команды безопасного отключения из Проводника или следующего сеанса GUI.
При следующем запуске менеджер перечисляет защищённые записи текущего
пользователя и принимает состояние только после успешной аутентифицированной
команды `ping` соответствующему host. Поэтому повторно запущенный GUI видит уже
подключённый диск, не создаёт второй экземпляр и может отправить безопасную
команду отключения без прежнего process handle. Недействительные записи
удаляются вместе с устаревшим меню Проводника.

Установщик проверяет SHA-256 и подпись официальных пакетов WinFsp и WinSpd, включает
платформенную библиотеку в `CleverPGP.exe` и устанавливает драйвер вместе с
программой. Виртуальный блочный backend проходит автоматические испытания подключения,
чтения, записи и внешнего отключения. Текущий WinFsp-backend сохраняется как
отдельный рабочий вариант, пока новые блочные операции проходят поэтапную
проверку на реальных временных дисках.

Первое форматирование нового виртуального диска вынесено из GUI в отдельный
скрытый процесс с повышенными правами. Одноразовый запрос находится только в
каталоге текущего пользователя, имеет строгий путь и набор полей, содержит
DPAPI-защищённый токен живого host и имитозащиту всех параметров. Помощник
отказывается работать без прав администратора, дважды выполняет
аутентифицированный `ping`, заново получает список дисков и требует полного
совпадения номера, размера, имени, серийного и уникального идентификаторов.
Непосредственно перед `Format-Volume` повторно проверяются MBR, единственный
раздел, его смещение, а также запрет системного и загрузочного диска. Пароль,
мастер-ключ, ключ тома и биометрические данные через UAC-границу не передаются.
Если разрешение отменено до первого форматирования, созданный пустой образ
удаляется как незавершённая операция.

Менеджер виртуального диска уже объединяет создание `.cpgv`, подключение
фонового процесса, поиск ровно одного нового устройства, форматирование и
повторное подключение ранее созданного тома по назначенной Windows букве.
Названия томов на русском, казахском и английском передаются в PowerShell в
Base64-представлении, поэтому пользовательский текст не интерполируется в
системную команду.

Обычное окно создания определяет доступность установленных WinSpd и WinFsp и
показывает только работающие варианты. Быстрый виртуальный Windows-диск выбран
первым и передаёт путь, ёмкость, название и файловую систему менеджеру
жизненного цикла. Менеджер создаёт блочный контейнер, подключает устройство,
форматирует выбранную NTFS или exFAT и возвращает назначенную Windows букву.
Универсальный WinFsp-backend не удалён и остаётся доступным в том же окне.
Интерфейс рекомендует журналируемую NTFS для постоянной работы в Windows и
объясняет, что совместимая exFAT менее устойчива к внезапному отключению.
Минимальная ёмкость виртуального диска — 32 МиБ.

Расширение уже увеличенного блочного контейнера выполняется только для NTFS.
Минимальный процесс с повышенными правами получает одноразовый запрос без
ключей и паролей. Случайный токен запроса защищён DPAPI, а точный снимок диска
аутентифицирован HMAC; файл запроса удаляется после единственного чтения.
Помощник требует права администратора, находит живую DPAPI-защищённую запись и
дважды выполняет аутентифицированный `ping` host-процесса. Перед
`Resize-Partition` он повторно сравнивает номер раздела, букву, имя, серийный
номер, уникальный идентификатор, тип шины, размер и смещение, запрещает
загрузочные и системные диски и требует MBR с единственным разделом NTFS.
Встроенное расширение exFAT Windows не поддерживается; поэтому для exFAT
операция завершается без изменения данных. Команда регистрируется только для
активной буквы диска и открывает компактное окно аутентификации.

Команда `--unmount` сначала проверяет служебный файл текущего WinFsp-диска, а
для виртуального NTFS/exFAT-диска находит защищённую запись текущего пользователя
и отправляет аутентифицированную команду его фоновому процессу. После
исчезновения буквы диска запись удаляется. Это позволяет использовать один
пункт безопасного отключения для обоих backend без передачи секретов через
командную строку.

Команда `--settings <drive>` передаёт букву, выбранную в Проводнике, и
повторно сверяет её с защищённой записью живого host-процесса. До окончания
аутентификации настройки недоступны. Главная панель не создаётся: показываются
только компактная форма входа, настройки профиля, числовой прогресс и результат.
Закрытие этого вспомогательного процесса очищает ключ сеанса, но не посылает команду
отключения диска.
